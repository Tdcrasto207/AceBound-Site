# Netlify build settings for a plain static HTML site.
# No build step: the repository root is published as-is.
[build]
  publish = "."
  command = ""

# Clean URL for the niches page
[[redirects]]
  from = "/niches"
  to = "/niches.html"
  status = 200
